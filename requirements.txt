fastapi
uvicorn
pymongo
pydantic
pandas

# For the LSTM behavioral model.
# (torch, joblib, matplotlib match what train_autoencoder_local.py /
# evaluate_model.py already needed for the Autoencoder half.)
torch
joblib
matplotlib
scikit-learn

# For shap_evaluate_fraud.py / shap_evaluate_lstm.py (offline SHAP
# validation of the fast reconstruction-error attribution). Not needed
# for the live API -- only for the report evaluation scripts.
shap
scipy